
<h2><%= @cocktail.name%></h2>


<%= link_to "Add a Dose",  new_cocktail_dose_path(params[:id])%>

 <% @doses.each do |dose| %>
 <%= dose.description %>
 <%= Ingredient.find(dose.ingredient_id).name%>
 <% end %>
